// The model called the same tool twice — observed when a small model loses
// track of what it already ran and re-derives the same conclusion.
export const repeatCall = '{"tool": "VerifyGeneticMarker", "arguments": {"geneMarker": "TP53"}, "done": false}';
