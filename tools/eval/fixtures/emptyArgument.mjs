// A small model asked for a required argument and skipped it — a real
// observed reply shape, not a hypothetical.
export const emptyArgument = '{"tool": "VerifyGeneticMarker", "arguments": {"geneMarker": ""}, "done": false}';
