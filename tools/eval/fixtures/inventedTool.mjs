// The model invented a tool that doesn't exist in this diagram at all —
// a hallucinated name, not a typo of a real one. Must activate nothing and
// must not be "corrected" to the nearest real tool name.
export const inventedTool = '{"tool": "LookupShipmentHistory", "arguments": {}, "done": false}';
